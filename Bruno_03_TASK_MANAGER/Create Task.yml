info:
  name: Create Task
  type: http
  seq: 3

http:
  method: POST
  url: "{{main}}"
  body:
    type: json
    data: |-
      {
        "name": "Visitor",
        "completed": false
        
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
